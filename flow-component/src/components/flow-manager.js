import React from "react"
import Container from "@material-ui/core/Container"

const FlowManager = ({options}) => {
  console.log(options)

  return (
    <Container/>
  )
}

export default FlowManager
