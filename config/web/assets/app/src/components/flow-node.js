import React from "react"
import { withStyles } from "@material-ui/core/styles"

const styles = (theme) => ({
  root: {
  },
})

const FlowNode = (props) => {

  return (
    <div>FlowNode</div>
  )
}
