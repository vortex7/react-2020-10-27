import React from "react"
import { withStyles } from "@material-ui/core/styles"

const styles = (theme) => ({
  root: {
  },
})

const FlowManager = (props) => {

  return (
    <div>FlowManager</div>
  )
}
